root_dir = "#{File.expand_path(File.dirname(__FILE__))}/.."

# Run setup for the platform needed platform
desc "Setup"
before_all do |lane, options|
  case @platform
  when :ios
    ios_fastfile_dir = "#{root_dir}/legacy/ios/fastlane/Fastfile"
    UI.message("File exists at #{ios_fastfile_dir} == #{File.exists?(ios_fastfile_dir)}")
    import ios_fastfile_dir
  when :android
    UI.message("Setup for android is not implemented yet")
  when :flutter
    UI.message("Setup for flutter is not implemented yet")
  else
    UI.message("There is no platform \"#{@platform}\", exiting...")
    raise "Unknown platform"
  end
end