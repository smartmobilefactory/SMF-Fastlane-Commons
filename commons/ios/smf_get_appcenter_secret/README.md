### Get the Appcenter Secret 
This lane tries to extract the appcenter secret for the given build_variant.

Example:
```
smf_get_appcenter_secret(
  build_variant: 'alpha'    # The current build variant
)
```